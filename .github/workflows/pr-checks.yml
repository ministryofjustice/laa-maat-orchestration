name: PR Checks

on:
  pull_request:
    branches: [main]

jobs:
  run-pr-checks:
    runs-on: ubuntu-latest
    steps:
      - name: PR description check
        env:
          TITLE: ${{ github.event.pull_request.title }}
        run: |
          echo $TITLE
          exit 0
